# The URL the site will be built for
base_url = "https://superthevenin.github.io/"

# The site title and description; used in feeds by default.
title = "SuperThevenin"
description = "Need Something ?"

# The default language; used in feeds.
default_language = "en"

# The site theme to use.
theme = "apollo"

# For overriding the default ouput directory `public`, set it to another value (e.g.: "docs")
output_dir = "public"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

taxonomies = [{ name = "tags" }]

[params]
	mode="auto" # color-mode → light,dark,toggle or auto
	useCDN=false # don't use CDNs for fonts and icons, instead serve them locally.
	subtitle = "SuperThevenin BlogPost"
	mathjax = true # enable MathJax support
	katex = true # enable KaTeX support

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

[markdown]
highlight_code = true
transparent = true
highlight_theme = "ayu-dark"


[extra]
mode="auto"
copyright = "SuperThevenin"
theme = "toggle"
comment = false

menu = [
    { name = "/posts", url = "/posts", weight = 1 },
    { name = "/projects", url = "/projects", weight = 2 },
    { name = "/about", url = "/about", weight = 3 },
    { name = "/tags", url = "/tags", weight = 4 },
]


# config social icon info in the footer
socials = [
    { name = "Twitter", url = "https://twitter.com/superthevenin", icon = "twitter" },
    { name = "GitHub", url = "https://github.com/SuperThevenin", icon = "github" },
]
